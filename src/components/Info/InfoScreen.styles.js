import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  safeArea: {
    backgroundColor: 'white',
  },
  scrollContent: {
    backgroundColor: 'white',
  },
  container: {},
});
